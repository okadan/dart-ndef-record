name: ndef_record
description: A Dart package implementing the NFC Data Exchange Format (NDEF).
version: 1.4.2
repository: https://github.com/okadan/dart-ndef-record

environment:
  sdk: ^3.11.0

dependencies:
  collection: ^1.19.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
